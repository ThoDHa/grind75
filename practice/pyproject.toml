[project]
name = "grind75-practice"
version = "0.1.0"
description = "Practice harness for the grind75 problems"
requires-python = ">=3.11"
dependencies = ["pytest>=7"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["harness"]
