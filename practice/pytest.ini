[pytest]
pythonpath = .
addopts = -ra
testpaths = .
python_files = test_*.py
markers =
    simple: the LeetCode example cases ("Run") — quick feedback while practicing
    full: the comprehensive edge-case gauntlet ("Submit") — simple + full cases
